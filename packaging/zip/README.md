# ZIP packaging

Build a portable zip package:

```bash
./packaging/zip/build.sh
```

Custom output dir:

```bash
./packaging/zip/build.sh --output-dir /tmp/zip
```

Result:

- `dist/zip/porthound4_<version>-1.zip`
